# CoolBee 数字人调试报告

**时间**: 2026-02-19 16:00
**状态**: 渲染管线工作正常，但内容不可见

---

## 一、已完成的修复

### 1.1 Surface 回调修复 ✅

修复了当 surface 已经可用时回调不触发的问题。

### 1.2 可见性逻辑修复 ✅

修复了 `showDigitalHumanWithFlyIn()` 的检查逻辑。

### 1.3 模型加载验证 ✅

模型正确加载，12 个 morph targets 正确映射。

---

## 二、当前问题

### 2.1 渲染内容不可见

**现象**:
- ✅ 渲染循环正常运行 (60fps)
- ✅ Surface 可用并正确尺寸 (465x583)
- ✅ 模型加载成功 (1 entity, 12 morphs)
- ✅ 相机位置正确设置
- ❌ **屏幕上什么都看不到（纯黑）**

**已尝试的测试**:
1. CoolBee 模型 - 不可见
2. 简单立方体模型 - 不可见
3. 不同相机位置 - 不可见
4. 不使用 transformToUnitCube - 不可见

**可能原因**:
1. ModelViewer 的 SwapChain 没有正确连接到 TextureView
2. TextureView 的 surface 配置问题
3. Filament 渲染目标配置问题

---

## 三、下一步建议

### 3.1 检查 ModelViewer 用法

参考 filament-utils-android 官方示例，确认 ModelViewer 的正确使用方式。

可能需要:
- 检查是否需要手动创建 SwapChain
- 检查 TextureView 的配置
- 检查渲染目标的设置

### 3.2 简化测试

创建一个最小的 Filament 渲染测试，不使用 ModelViewer，直接使用 Filament API 进行渲染，以确认基础渲染功能正常。

### 3.3 检查 filament-utils 版本

确认使用的 filament-utils-android 版本是否与项目兼容。

---

## 四、日志摘要

```
FilamentRenderer: Surface available: 465x583
FilamentRenderer: Loading model from sdcard
FilamentRenderer: Added 1 entities to scene
FilamentRenderer: Model loaded: 0 morphs, 0 animations (立方体)
FilamentRenderer: Camera lookAt: eye=(0,2,5), center=(0,0,0)
FilamentRenderer: Rendering started
FilamentRenderer: Rendering: 60 frames, fps=59.88
```

---

## 五、结论

**渲染管线**表面工作正常（60fps 渲染），但 **TextureView 没有显示渲染内容**。这是一个底层的 Filament/TextureView 集成问题，需要进一步调试。

**建议**:
1. 参考 filament-utils-android 官方示例重新检查代码
2. 或者使用纯 Filament API（不使用 ModelViewer）进行测试
3. 考虑使用 SurfaceView 替代 TextureView

---

**文件变更**:
- `FilamentRenderer.kt` - 多次修改尝试修复渲染问题
- `MainActivity.kt` - 可见性逻辑修复

**模型文件**:
- `/sdcard/iptv-agent-models/digital-human/avatar.glb` - 测试立方体
